%h1 Retrieve a report
- if @selected_account.nil?
  .notice No account selected!
- else
  See
  %a{:href => "https://developers.google.com/adwords/api/docs/appendix/reports"}
    reporting documentation
  for reports and fields reference. The report will
  be ran for the last 7 days.
  %br
    - if @reports.empty?
      %ul
        %li No reports available.
    - else
      .report-form
        = form_tag('/report/get', :method => 'post') do
          .report-form-item
            Report:
            %select{:name => "type"}
              - @reports.each_pair do |key, report|
                %option{:value => key}
                  = report.name
          .report-form-item
            Fields:
            %input{:name => "fields", :size => "80", :value => "ExternalCustomerId, AccountDescriptiveName"}
          .report-form-item
            Format:
            %select{:name => "format"}
              - @formats.each_pair do |key, format|
                %option{:value => key}
                  = format.name
          .report-form-item
            %input{:name => "zeroes", :type => "checkbox", :value => "true"}
              Include zero impressions
          .report-form-item
            %input{:type => "submit", :value => "Get report"}
#local-footer
  %span.small-print= link_to("Back to home", home_index_path)