.form-group.devise-form
  %h1 Sign Up Profile
  = simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
    = f.error_notification
    .form-inputs
      = f.input :username, required: true, input_html: {class: 'form-control'}, label: false, placeholder: 'Username'
      = f.input :email, required: true, input_html: {class: 'form-control'}, label: false, placeholder: 'Email'
      = f.input :password, required: true, input_html: {class: 'form-control'}, label: false, placeholder: 'Password'
      = f.input :password_confirmation, required: true, input_html: {class: 'form-control'}, label: false, placeholder: 'Password Confirmation'
    .form-actions
      .row
        .col-lg-6.col-md-6.col-sm-12.col-xs-12
          = link_to "Login", new_user_session_path, class: 'btn btn-main'
        .col-lg-6.col-md-6.col-sm-12.col-xs-12
          = f.button :submit, "Sign up", class: 'btn btn-main'